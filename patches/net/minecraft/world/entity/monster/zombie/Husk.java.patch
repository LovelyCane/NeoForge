--- a/net/minecraft/world/entity/monster/zombie/Husk.java
+++ b/net/minecraft/world/entity/monster/zombie/Husk.java
@@ -70,6 +_,7 @@
 
     @Override
     protected void doUnderWaterConversion(ServerLevel p_455883_) {
+        if (!net.neoforged.neoforge.event.EventHooks.canLivingConvert(this, EntityType.ZOMBIE, (timer) -> this.conversionTime = timer)) return;
         this.convertToZombieType(p_455883_, EntityType.ZOMBIE);
         if (!this.isSilent()) {
             p_455883_.levelEvent(null, 1041, this.blockPosition(), 0);
